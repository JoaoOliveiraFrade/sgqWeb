const local = 'http://localhost:8080#'
const homologation = 'http://sgqhml.intranet/dist/index.html#'
const production = 'http://sgq.intranet/dist/index.html#'

const webAppPath = {
  local,
  homologation,
  production,
  default: local
}

export default webAppPath
